import React from "react";
import { useNavigate } from "react-router-dom";

const PendingOrders = () => {
    return(
        <div>
            <h1>Pending Orders</h1>
            <p>Welcome to the pending orders management page.</p>
        </div>
    )
}
export default PendingOrders;